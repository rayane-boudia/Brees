<?xml version="1.0" encoding="utf-8"?>
<!-- This file defines the gradient. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:type="linear"
        android:angle="-270"
        android:startColor="@color/bleuBrees"
        android:endColor="#000000" />

    <!--
      Explanation:
      - android:shape="rectangle": We are filling a rectangular area.
      - android:type="linear": Creates a linear (straight-line) gradient.
      - android:angle="270": Sets the angle of the gradient.
          0 is left-to-right
          90 is bottom-to-top
          270 is top-to-bottom (which matches your image)
      - android:startColor: The color at the top (a medium green).
      - android:endColor: The color at the bottom (black, or a very dark green).
    -->

</shape>